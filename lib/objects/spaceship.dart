import 'package:flutter/material.dart';

class Spaceship extends StatelessWidget{


  @override
  Widget build(BuildContext context){
    return Container(

        child: Image.asset(
            'assets/images/pixil-frame-0.png',
        )
    );


  }
}